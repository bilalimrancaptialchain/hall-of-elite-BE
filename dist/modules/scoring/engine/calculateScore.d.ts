import { ScoreResult, ScoringMetrics } from "./score.types";
/**
 * Calculate a weighted consistency score from normalized metrics.
 * Assumes the trader has already passed validation and eligibility.
 */
export declare const calculateScore: (metrics: ScoringMetrics) => ScoreResult;
//# sourceMappingURL=calculateScore.d.ts.map