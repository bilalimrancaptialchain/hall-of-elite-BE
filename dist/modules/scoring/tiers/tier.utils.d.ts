/**
 * Clamp a numeric score into [0, 100].
 */
export declare const clampScore: (score: number) => number;
/**
 * Check if a score falls within a [min, max] band (inclusive on both ends).
 */
export declare const isScoreInBand: (score: number, minScore: number, maxScore: number) => boolean;
//# sourceMappingURL=tier.utils.d.ts.map